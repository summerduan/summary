<j:jelly xmlns:j="jelly:core" xmlns:st="jelly:stapler"
	xmlns:d="jelly:define" xmlns:l="/lib/layout" xmlns:t="/lib/hudson"
	xmlns:f="/lib/form" xmlns:i="jelly:fmt">
  <st:header name="Content-Type" value="text/html;charset=UTF-8" />
  <table class="pane sortable" id="warnings">
    <tr>
      <td class="pane-header" width="80%">文件名</td>
      <td class="pane-header" width="20%">风险数</td>
    </tr>
    <tbody>
      <j:forEach var="filename" items="${it.getKeySet()}">
      	<j:set var="detail" value="${it.data.filestatistics[filename]}" />
      	<j:if test="${detail.bugnum > 0}">
	        <tr>
	          <td class="pane">
	            <a href="./source:${filename}/?">${filename}</a>
	          </td>
	          <td class="pane">
	            ${detail.bugnum}
	          </td>
	        </tr>
      	</j:if> 
      </j:forEach>
    </tbody>
  </table>
</j:jelly>
